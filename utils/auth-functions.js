import config from "../client-config";

/**
 * Check if user is logged in.
 *
 * @return {object} Auth Object container token and user data.
 */
export const isValidated = () => {

	const authTokenData = localStorage.getItem( config.authTokenName );
	if (  ) {

	}

};
